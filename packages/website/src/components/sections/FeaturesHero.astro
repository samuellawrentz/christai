---
import { ArrowRight } from '@lucide/astro';
---

<section class="relative min-h-[70vh] flex items-center overflow-hidden texture-overlay">
  <!-- Background -->
  <div class="absolute inset-0 bg-[var(--color-parchment)]" />

  <!-- Decorative blurs -->
  <div class="absolute top-20 right-10 w-64 h-64 bg-[var(--color-ochre)]/10 rounded-full blur-3xl" />
  <div class="absolute bottom-20 left-20 w-96 h-96 bg-[var(--color-terracotta)]/10 rounded-full blur-3xl" />

  <div class="container mx-auto px-6 lg:px-8 relative z-10 py-24 lg:py-32">
    <div class="max-w-4xl mx-auto text-center">
      <!-- Eyebrow -->
      <p class="text-[var(--color-ochre)] text-sm tracking-[0.3em] uppercase mb-6 animate-fade-in">
        Designed for Your Faith Journey
      </p>

      <!-- Main headline -->
      <h1 class="text-5xl md:text-6xl lg:text-7xl text-[var(--color-ink)] leading-[0.95] mb-8 animate-fade-in" style="animation-delay: 0.1s">
        Powerful <span class="italic text-[var(--color-ochre)]">Features</span><br/>
        for Meaningful Conversations
      </h1>

      <!-- Subheadline -->
      <p class="text-xl md:text-2xl text-[var(--color-warm-gray)] max-w-2xl mx-auto mb-12 leading-relaxed animate-fade-in" style="animation-delay: 0.2s">
        Every tool designed to deepen your connection with Scripture and the wisdom of biblical figures.
      </p>

      <!-- CTA -->
      <div class="flex flex-col sm:flex-row gap-4 items-center justify-center animate-fade-in" style="animation-delay: 0.3s">
        <a
          href="https://app.christianai.world"
          target="_blank"
          rel="noopener noreferrer"
          class="btn-sacred inline-flex items-center gap-3"
        >
          Start Your Journey
          <ArrowRight class="w-5 h-5" />
        </a>
        <span class="text-[var(--color-stone)] text-sm">
          Free to use
        </span>
      </div>

      <!-- Sacred divider -->
      <div class="sacred-divider max-w-xs mx-auto mt-16">
        <span class="text-2xl">&#10041;</span>
      </div>
    </div>
  </div>
</section>
